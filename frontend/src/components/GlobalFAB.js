import React from 'react';

/**
 * @deprecated New Task FAB replaced by the persistent GlobalAIDock.
 * Kept as a no-op export so older imports do not break builds.
 */
const GlobalFAB = () => null;

export default GlobalFAB;
